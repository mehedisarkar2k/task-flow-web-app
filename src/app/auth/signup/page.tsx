import { SignupScreen } from "@/screens/auth/signup-screen";

export const metadata = {
  title: "Sign up | TaskFlow",
  description: "Create your TaskFlow workspace account.",
};

const SignupPage = () => {
  return <SignupScreen />;
};

export default SignupPage;
